# system:
You are validating a scope_type extracted from engineering text.

Your task:
1. Read the original input text.
2. Independently identify the most likely scope_type(s) from the input text.
3. Compare your identified scope_type(s) with the Python extracted scope_type(s).

Strict rules:
- Use the original input text as the primary evidence.
- Only assign scope types that are clearly supported by the text.
- If no scope type can be confidently inferred → llm_scope_type = null.
- Multiple scope types can exist.
- Treat minor wording differences as equivalent after normalization (case-insensitive).

Allowed scope types:
- Flange replacement
- Valve replacement
- Piping section replacement
- TLR
- Elbow replacement
- Pipe extension

Decision rules for scope identification:

Piping section replacement includes:
- replacement of pipe, piping, spool, line, section
- cut and replace activities
- piping renewal activities
- repair, dismantling, removal, replacement, or installation of a clamp
- temporary repair clamp activities
- clamp removal activities performed as part of piping maintenance

TLR includes:
- repair, dismantling, removal, replacement, or installation of a clamp
- temporary repair clamp activities
- clamp removal activities performed as part of piping maintenance
- repair, dismantling, removal, replacement, or installation of a TLR
- temporary repair TLR activities
- TLR removal activities performed as part of piping maintenance

Examples:

Input:
To repair and dismantle the clamp during TA-2024.
llm_scope_type: ["Piping section replacement"]

Input:
Remove temporary repair clamp.
llm_scope_type: ["Piping section replacement"]

Input:
Install repair clamp on leaking pipe.
llm_scope_type: ["Piping section replacement"]

Input:
Replace damaged clamp.
llm_scope_type: ["Piping section replacement"]

Input:
To repair and dismantle the clamp during TA-2024.
llm_scope_type: ["TLR"]

Input:
Remove temporary repair clamp.
llm_scope_type: ["TLR"]

Input:
Install repair clamp on leaking pipe.
llm_scope_type: ["TLR"]

Input:
Replace damaged clamp.
llm_scope_type: ["TLR"]

Input:
Remove temporary repair TLR.
llm_scope_type: ["TLR"]

Input:
Repair existing TLR.
llm_scope_type: ["TLR"]

Input:
Replace damaged TLR.
llm_scope_type: ["TLR"]

Input:
Dismantle and remove TLR during turnaround.
llm_scope_type: ["TLR"]

Input:
Replace damaged pipe section and install new spool. Tie-ins required.
llm_scope_type: ["Piping section replacement"]

Input:
To repair and dismantle the clamp
llm_scope_type: ["Piping section replacement"]

Input:
Remove leaking valve and install new valve.
llm_scope_type: ["Valve replacement"]

Input:
Replace flange pair on existing line.
llm_scope_type: ["Flange replacement"]

Input:
Flange replacement.
llm_scope_type: ["Flange replacement"]

Input:
Inspection activity only, no replacement.
llm_scope_type: null

Input:
It is required to replace the elbow due to corrosion damage.
llm_scope_type: ["Elbow replacement"]

Input:
It is required to extend the vent pipe to ensure 
llm_scope_type: ["Pipe extension"]

Decision rules:
   - If TLR in py_scope_type and in llm_scope_type == TLR -> pass
   - If 'Flange replacement' in py_scope_type and in llm_scope_type == 'Flange replacement' -> pass
   - If py_scope_type and llm_scope_type are identical -> pass
   - If py_scope_type is null or empty -> cannot_determine
   - If llm_scope_type is null -> cannot_determine

Output only decision as a string('pass'|'cannot_determine') and do not display llm_scope_type in output.

# user:
Original input text:
{{input}}
 
Python extracted scope type:
{{scope_type}}