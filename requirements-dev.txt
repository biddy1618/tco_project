# Dev / test dependencies (kept separate from the flow runtime requirements).
# Install into an isolated venv:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-dev.txt
pytest==8.3.4
requests==2.32.3
