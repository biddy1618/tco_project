# system:
You are a helpful assistant.

# user:

Output:
Take the content part and display as a table

{{question}}

{# The user may provide a single size value (example: "size 20") or a range ("size 10-12").

Extract the size value from {{input}}

A) If it is single size: 
Extract it, then compare with all items size.
If it is in range, then display.
If not, ignore

B) If it is a size in range A-B:
Display {{question}}



Output:

Display as a table #}

{# Strict Rule:

Do not reveal yourself #}