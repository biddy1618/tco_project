# Foundry hosted-agent extras. Isolated env only (venv / Docker).
# Do not pip-install this into the Prompt Flow interpreter.
#
# CLI slices stay on maf/requirements.txt (agent-framework==1.14.0).
# This file may pull a newer agent-framework if the hosting package needs it.
#
# Corporate Artifactory may not have the prerelease hosting package. If
# `pip install` fails, use the same index that already has agent-framework,
# or install from public PyPI in an isolated env on a machine that allows it.

azure-identity>=1.25.0
agent-framework-foundry
agent-framework-foundry-hosting
